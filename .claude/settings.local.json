{
  "permissions": {
    "allow": [
      "Bash(PYTHONPATH=. uv run python -c \"\nfrom src.backend.services.agent_service import create_agent\nfrom agents.mcp import MCPServerStdio\n\n# Just test that agent can be created without errors\nprint(''Testing agent creation...'')\ntry:\n    # We can''t create full agent without MCP server, but we can check imports\n    from src.backend.tools.finnhub_tools import get_stock_quote\n    print(''✅ Agent service imports work correctly'')\n    print(''✅ get_stock_quote tool available'')\nexcept Exception as e:\n    print(f''❌ Error: {e}'')\n\")",
      "Bash(PYTHONPATH=. uv run python:*)"
    ],
    "deny": [],
    "ask": [],
    "additionalDirectories": [
      "/home/anthony/.claude"
    ]
  },
  "enableAllProjectMcpServers": true,
  "enabledMcpjsonServers": [
    "sequential-thinking",
    "context7"
  ]
}
