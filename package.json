{
  "name": "market-parser-polygon-mcp",
  "version": "1.0.0",
  "description": "Market Parser - Python CLI and Gradio UI for financial queries using Polygon.io MCP server and OpenAI GPT-5-mini",
  "private": true,
  "type": "module",
  "scripts": {
    "// === DEVELOPMENT COMMANDS ===": "",
    "dev": "npm run backend:dev",
    "start": "npm run dev",
    "// === BACKEND COMMANDS ===": "",
    "backend:dev": "uv run uvicorn src.backend.main:app --host 127.0.0.1 --port 8000 --reload",
    "backend:server": "uv run -m src.backend.main --server",
    "backend:cli": "uv run -m src.backend.main",
    "backend:install": "uv install",
    "// === CODE QUALITY COMMANDS ===": "",
    "lint": "npm run lint:python",
    "lint:python": "uv run pylint src/backend/ tests/",
    "lint:fix": "uv run black src/backend/ tests/ --line-length 100 && uv run isort src/backend/ tests/ --profile black --line-length 100",
    "// === INSTALLATION & MAINTENANCE ===": "",
    "install:all": "npm install",
    "install:backend": "npm run backend:install",
    "clean": "rm -rf node_modules dist test-results",
    "clean:cache": "rm -rf .cache node_modules/.cache",
    "clean:install": "npm run clean && npm run install:all",
    "clean:full": "npm run clean && npm run clean:cache && npm run install:all",
    "// === ONE-CLICK STARTUP COMMANDS ===": "",
    "start:app": "bash chmod +x start-app.sh && ./start-app.sh",
    "start:app:xterm": "bash chmod +x start-app-xterm.sh && ./start-app-xterm.sh",
    "// === UTILITY COMMANDS ===": "",
    "status": "echo '=== Backend Status ===' && (curl -s http://localhost:8000/health || echo 'Backend not running') && echo && echo '=== Gradio UI Status ===' && (curl -s http://localhost:7860 > /dev/null && echo 'Gradio UI running on http://localhost:7860' || echo 'Gradio UI not running')",
    "health": "npm run status",
    "reset": "npm run clean:full && npm run dev"
  },
  "dependencies": {
    "@playwright/test": "^1.55.0"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.45.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/market-parser-polygon-mcp.git"
  },
  "keywords": [
    "financial-analysis",
    "polygon-io",
    "openai",
    "gradio",
    "fastapi",
    "mcp-server",
    "stock-market",
    "cli",
    "python-ui"
  ],
  "author": "Market Parser Team",
  "license": "MIT"
}